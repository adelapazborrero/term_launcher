<?xml version="1.0" encoding="utf-8"?>
<!-- Backup / device-transfer rules for Android 12+. Includes all SharedPreferences
     (theme, settings, favorites, folders, aliases) for both cloud backup and
     device-to-device transfer. -->
<data-extraction-rules>
    <cloud-backup>
        <include domain="sharedpref" path="." />
    </cloud-backup>
    <device-transfer>
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
